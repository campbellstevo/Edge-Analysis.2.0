# Pinned 24 Sep 2026 to the set the six-view render test passes on
# (tests/test_render_views.py). Streamlit 1.47.1 is what the deployed app
# runs; an unpinned rebuild would pull a newer one untested.
# To upgrade: change a pin, run the tests, then push. A changed line here
# is what forces Streamlit Cloud to rebuild — no "# rebuild N" lines needed.
streamlit==1.47.1
altair==5.4.1
pandas==2.3.3
numpy>=1.26,<3          # compiled: range so it resolves on Cloud's Python
scipy>=1.11,<2          # compiled: range so it resolves on Cloud's Python
pillow==11.3.0          # imported by theme.py
python-dateutil==2.9.0.post0
requests==2.32.5
notion-client==2.2.1
sentry-sdk==2.35.0
streamlit-js-eval==0.1.9
qrcode==7.4.2
fpdf2==2.7.9
